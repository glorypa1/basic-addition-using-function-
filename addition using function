#include <stdio.h>
int add(int a, int b); // declaration 
int main()
{
int a,b, c;
printf("Enter value of a and b");
scanf("%d%d", &a,&b);
c=add(a,b);
printf ("c=%d",c);
return 0;
}
int add(int a, int b)
{
int d;
d=a+b;
return d;
}
